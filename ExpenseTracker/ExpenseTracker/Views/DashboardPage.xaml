<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             x:Class="ExpenseTracker.Views.DashboardPage"
             Title="Dashboard">
    <VerticalStackLayout Padding="20" Spacing="20">

        <Label Text="Dashboard"
               FontSize="28"
               FontAttributes="Bold"/>
        
        <Border BackgroundColor="#2E2E2E" Padding="15">
            <Label Text="{Binding Balance, StringFormat='Balance: {0:C}'}"
                       TextColor="{Binding BalanceColor}"
                       FontSize="26"
                       FontAttributes="Bold"/>
        </Border>

        <Border BackgroundColor="#1F3B2C" Padding="15">
            <Label Text="{Binding TotalIncome, StringFormat='Income: {0:C}'}"
                   TextColor="{Binding IncomeColor}"
                   FontSize="20"/>
        </Border>

        <Border BackgroundColor="#3B1F1F" Padding="15">
            <Label Text="{Binding TotalExpenses, StringFormat='Expenses: {0:C}'}"
                   TextColor="{Binding ExpensesColor}"
                   FontSize="20"/>
        </Border>

        <lvc:PieChart Series="{Binding Series}"
                      HeightRequest="300"/>
    </VerticalStackLayout>
</ContentPage>